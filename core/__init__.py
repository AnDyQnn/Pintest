"""
core — стековый движок аудита (форк pintest/audit.py, который правим свободно).

Оригинал `pintest/audit.py` остаётся нетронутым эталоном для отдельной выкачки.
Здесь тот же код + чистая библиотечная поверхность для оркестратора хоста и агента:

  core.auditor    — полный движок (пайплайн + nmap + отчёты); можно гонять как CLI
  core.targets    — чистка/валидация «грязного» списка целей (для UI-загрузки)
  core.reporting  — сборка сводного отчёта из накопленных results.jsonl (для хоста)
"""
