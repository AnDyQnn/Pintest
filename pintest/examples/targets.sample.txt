# Пробный список целей для audit.py — ТОЛЬКО легальные мишени.
# Формат: одна цель на строку. '#' — комментарий. Пустые строки игнорируются.
#
# scanme.nmap.org — хост, который Nmap официально разрешает сканировать для учёбы.
scanme.nmap.org
#
# 127.0.0.1 — твой собственный localhost (всегда можно сканировать себя).
127.0.0.1
#
# --- Сюда добавляй ТОЛЬКО то, на что есть разрешение (scope задания / свои хосты) ---
# Поддерживаемые форматы (скрипт сам приведёт к виду для nmap):
# 192.0.2.10                    # одиночный IP
# 192.0.2.0/24                  # CIDR (адрес + маска)
# 192.0.2.10-50                 # диапазон в последнем октете
# 192.0.2.0-192.0.2.255         # полный-полный диапазон -> станет 192.0.2.0-255
# 2001:db8::1                   # IPv6 (только конкретные адреса)
