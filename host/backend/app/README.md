# host/backend/app/ — Python-пакет backend

Собственно код control plane (FastAPI). Разбор каждого модуля (`main`, `orchestrator`,
`agents`, `vpn`, `exploitation`, `loot`, `topology`, `console`, …) — в родительском
[`../README.md`](../README.md). Все настройки — в `config.py` (через ENV).
